#ifndef DBAH_DEFINITIONS_H
#define DBAH_DEFINITIONS_H

#include "SDL.h"
#include "SDL_ttf.h"
#include "SDL_image.h"
#include <cstring>			// memset
#include <string>
#include <memory>			// std::unique_ptr, std::shared_ptr, std::weak_ptr
#include <ios>
#include <fstream>
#include <random>
//#include <limits>
//#include <queue>
#include <math.h>

#include <unordered_map>
#include <array>
#include <algorithm>		// std::make_heap

#define BIT(x) 1 << x
#define DEG2RAD(angle) ( angle * ((float)(M_PI)/180.0f) )
#define RAD2DEG(radians) ( radians * (180.0f/((float)M_PI)) )

#endif /* DBAH_DEFINITIONS_H */